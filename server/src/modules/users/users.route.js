/*const {Router} = require('express');
const {authenticate, authorize} = require('../../middlewares/auth.middleware.js');
const {getMyCampaigns, createCampaign} = require('./user.controller.js');
const userRoute = Router();

userRoute.use(authenticate);

userRoute.get('/', authorize('creator'), getMyCampaigns); //check
userRoute.post('/', authorize('creator'), createCampaign);//check
//editar campaha
//eliminar campaha
//invitar colabs
//remover colabs 
module.exports = userRoute;

todo esto fue al dope pero booee de aqui nos llevamos cositas para el modulo de campañas

*/